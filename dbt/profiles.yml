hospital:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS', '') }}"
      project: "{{ env_var('GCP_PROJECT_ID', '') }}"
      dataset: ""
      threads: 4
      timeout_seconds: 300
      location: "{{ env_var('GCS_LOCATION', 'US') }}"
      priority: interactive
